/** Max rows fetched/rendered in the drill-down raw table preview. */
export const TABLE_PREVIEW_LIMIT = 500;

/** Max rows fetched for scatter, correlation, histogram (point density). */
export const ROW_CHART_SAMPLE_LIMIT = 5000;
